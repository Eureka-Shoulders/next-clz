import TYPES from '@containers/global.types';
import { inject, injectable } from 'inversify';

import { HttpService, Repository } from '@euk-labs/fetchx';

@injectable()
class <%= props.resourceNameCapitalized %>Repository extends Repository {
  constructor(@inject(TYPES.ApiService) apiService: HttpService) {
    super(apiService, { path: '/<%= props.resourceName %>' });
  }
}

export default <%= props.resourceNameCapitalized %>Repository;
